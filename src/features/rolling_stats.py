"""Rolling statistical features (mean, std, skew, kurtosis, z-score)."""
from __future__ import annotations

import pandas as pd


def add_rolling_stats(df: pd.DataFrame, windows) -> pd.DataFrame:
    """Add rolling summary statistics over the given windows. TODO: implement."""
    raise NotImplementedError
