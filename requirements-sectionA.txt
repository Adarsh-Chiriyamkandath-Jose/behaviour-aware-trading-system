# Lean dependency set for Section A (data + features + behavioral).
# Use this to get up and running with a small footprint:
#     pip install -r requirements-sectionA.txt
# The full stack (xgboost, shap, streamlit, ...) lives in requirements.txt and
# is only needed once modeling/backtest/app work begins.
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyyaml>=6.0
yfinance>=0.2.40
scikit-learn>=1.3
matplotlib>=3.7
pytest>=7.4
